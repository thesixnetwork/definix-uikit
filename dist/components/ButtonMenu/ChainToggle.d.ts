import React from "react";
import { ChainToggleProps } from "./types";
export declare const scalesValues: {
    sm: {
        height: number;
        fullWidth: number;
        minWidth: number;
    };
    md: {
        height: number;
        fullWidth: number;
        minWidth: number;
    };
};
declare const ChainToggle: React.FC<ChainToggleProps>;
export default ChainToggle;
